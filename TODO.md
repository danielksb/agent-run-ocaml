1. add "--model"/"-m" parameter to select the model to use
2. add "--debug"/"-d" parameter which prints all debug information to stderr. 
3. add "--verbose"/"-v" parameter which prints intermediate results from the model to stdout. This should allow the user to see which tools are called. All verbose messages start with "INFO:"
