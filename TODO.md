# TODOs

## Features
- add rate limit
- add param which allows loading files into context
- put skills into separate message instead of system prompt

## Refactorings
- Put skill registry into agents
- Config.load should return Error instead of throwing exception
- validate skill parameters in separate function
- use Arg to parse arguments
